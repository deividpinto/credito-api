# Desativa a substituição por banco embutido
spring.test.database.replace=none

# Configuração para PostgreSQL local
spring.datasource.url=jdbc:postgresql://localhost:5432/teste_credito_db
spring.datasource.username=adminteste
spring.datasource.password=teste123
spring.datasource.driver-class-name=org.postgresql.Driver

# Configuração h2
#spring.datasource.url=jdbc:h2:mem:testdb;DB_CLOSE_DELAY=-1
#spring.datasource.driverClassName=org.h2.Driver
#spring.datasource.username=sa
#spring.datasource.password=

# Configurações do Hibernate
spring.jpa.hibernate.ddl-auto=create-drop
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true

# Criar banco se não existir
spring.datasource.initialization-mode=always
